app = "fintheon-news-worker"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile.news-worker"

[env]
  NODE_ENV = "production"
  NEWS_WORKER_PORT = "8082"
  FLAG_NEWS_WORKER_WRITES_RISKFLOW = "true"

[http_service]
  internal_port = 8082
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"
    method = "GET"
    path = "/healthz"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024
