# v5.22 Session 1 — Desktop Refit + Backend Spine

## Context

You are the Desktop + Backend session of sprint v5.22. TP audited the live app on 2026-04-18 and returned a batch of changes. The scope has been split across two parallel Claude Code instances to run on fresh threads. A sibling session (Session 2) is handling all mobile work in parallel — you must not touch `mobile/`. The contracts you both depend on were already landed on branch `v5.22` at commit `06a777a` (`frontend/lib/fuse-palette.ts`, `frontend/lib/user-preferences.ts` and their mobile mirrors).

S26 already shipped (v.26.1 + v.26.2) — the catalyst modal + IV fuse drain/fill and maintenance backend are live. Do not re-do that work.

Read **all** of this brief before touching anything. Then read `src/lib/changelog.ts` top entries and `CLAUDE.md` before editing any file.

## Branch / Worktree / CWD

- **Worktree**: `/Users/tifos/Desktop/Codebases/fintheon-v5.22-s1`
- **Branch**: `v5.22-s1-desktop`
- **Parent**: `v5.22` at `06a777a`
- All work happens inside that worktree. `cd` into it first. When done, merge back into `v5.22` on the main worktree (`/Users/tifos/Desktop/Codebases/fintheon`).

## Scope — Included

### 1. Desktop Aquarium layout refit ([frontend/components/narrative/Sanctum.tsx](frontend/components/narrative/Sanctum.tsx))

Below the top "VOLATILITY READ" frame, the current layout compresses the KPI row with `max-w-2xl` + `flex justify-center` at lines 364-365, so MARKET HEAT / REGIME RISK / SIGNAL STRENGTH read as a narrow island while the futures row scrolls horizontally beneath. TP wants:

- Remove `max-w-2xl` and `flex justify-center` at [Sanctum.tsx:364-365](frontend/components/narrative/Sanctum.tsx:364). The KPI row now spans the same inline edges as the top frame.
- Apply the **dashboard page's margins** to every item in this section (the same horizontal breathing room as the dash — audit `frontend/components/dashboard/` or wherever the Consul dash lives and mirror its outer padding). Fill the main content area.
- Equal-thirds KPI layout, but **no vertical borderlines** between cells (TP explicitly said "all vertical borderlines removed, I want this section of the app to feel expansive"). Generous spacing does the separating.
- ANALYSIS paragraph gets its **own framed section** — same width as the KPI row and top frame, filling where it currently sits. Frame is a subtle accent-border glass surface (matches the top frame's treatment). Don't inline it into the KPI row.
- Align the /NQ /ES /YM /CL /GC header row in [AquariumPredictionCards.tsx:127-220](frontend/components/narrative/AquariumPredictionCards.tsx:127) so all five symbol headers sit on a **single horizontal line** — TP flagged current shuffled layout looks bad.

### 2. Nothing-Design flatten + 4.2s shimmer on ALL desktop fuses

Add this keyframe to [frontend/index.css](frontend/index.css) (somewhere near the existing fuse-shimmer at lines 152-334):

```css
@keyframes nothing-fuse-shimmer {
  0%,
  76%,
  100% {
    background-position: -200% 0;
    opacity: 0;
  }
  38% {
    background-position: 200% 0;
    opacity: 0.45;
  }
}
.nothing-fuse-shimmer {
  position: absolute;
  inset: 0;
  pointer-events: none;
  background: linear-gradient(
    90deg,
    transparent 0%,
    var(--fintheon-text, #f0ead6) 50%,
    transparent 100%
  );
  background-size: 200% 100%;
  mix-blend-mode: overlay;
  animation: nothing-fuse-shimmer 4.2s linear infinite;
}
@media (prefers-reduced-motion: reduce) {
  .nothing-fuse-shimmer {
    animation: none;
    opacity: 0;
  }
}
```

The `76%–100%` dwell gives ~1s of dead time between sweeps — TP wanted "slow" and no "AI sparkle." Do NOT reduce the dwell.

Apply `.nothing-fuse-shimmer` as an **absolutely-positioned overlay** inside these fuse fills:

- [BlendedVIXCard.tsx:86-111](frontend/components/narrative/BlendedVIXCard.tsx:86) — VIX / Headlines / MiroShark fuses (rename MiroShark label to "AGENT DESK")
- [NextSessionForecastCard.tsx:74-95](frontend/components/narrative/NextSessionForecastCard.tsx:74) — Confidence fuse
- [AquariumPredictionCards.tsx:162-172](frontend/components/narrative/AquariumPredictionCards.tsx:162) — HEAT bars on all 5 futures cards
- [frontend/components/RiskFlowMini.tsx](frontend/components/RiskFlowMini.tsx) — every fuse inside it
- [frontend/components/IVScoreCard.tsx](frontend/components/IVScoreCard.tsx) — every fuse inside it
- Any RiskFlow card component that renders a fuse (audit `frontend/components/feed/` + `frontend/components/narrative/` for fuse-like bars)
- Deliberation footer fuses (see section 3)

Fuse geometry change: replace `rounded-full` pill caps with `rounded-[2px]` (sharper Nothing geometry). Track bg becomes `var(--fintheon-surface)` opaque (not `bg-zinc-800`). Remove inner glows / backdrop blur on the fuse track.

### 3. Deliberation footer fuses

[frontend/components/consilium/DeliberationKPIOverlay.tsx](frontend/components/consilium/DeliberationKPIOverlay.tsx) (read it first). Add SIGNAL STRENGTH / REGIME RISK / MARKET HEAT as footer fuses on the Deliberation card (these move OFF the standalone KPI row in Sanctum per TP's instruction — the Sanctum section keeps MARKET HEAT / REGIME RISK / SIGNAL STRENGTH as the expansive top-pill values, and the Deliberation card also shows them as footer fuses for live read).

Actually re-read TP's message: _"Add the signal strength, Regime Risk, and Market Heat in as footer fuses on the Deliberation card."_ So add them there. Do not remove them from Sanctum — just replicate as fuses on Deliberation.

### 4. Econ Intel left + Instrument fuses right

TP: _"place the Econ Intelligence Section and on a left side-by side, with compact Instrument fuses on the right hand side."_

- Left: existing [frontend/components/narrative/SanctumEconIntel.tsx](frontend/components/narrative/SanctumEconIntel.tsx) unchanged in scope, placed in a left column.
- Right: new compact "Instrument fuses" panel. Each instrument from the futures row (/NQ /ES /YM /CL /GC) rendered as a vertical compact fuse with symbol label + live score. Think dense grid, right-column of the same section.
- Parent container in Sanctum.tsx wraps both in a `flex gap-4` with the econ side claiming ~60% and instrument fuses ~40%.

### 5. Desktop RiskFlow cards match mobile design (larger)

TP: _"refactor the RiskFlow cards to look like the mobile ones, but larger, while respecting severity and priority coloring set by user in Desktop settings."_

- Reference the mobile design: `mobile/components/riskflow/RiskFlowCard.tsx` (READ-ONLY reference).
- Rebuild the desktop RiskFlow card to the same visual vocabulary but larger — keep the mobile's header/meta/fuse/severity-tint pattern. Replace any current RiskFlow desktop cards (inside `frontend/components/RiskFlowMini.tsx` or a dedicated RiskFlow card component) with this shape.
- Colors must come from `colorForSeverity` / `colorForPriority` in [frontend/lib/fuse-palette.ts](frontend/lib/fuse-palette.ts) — do NOT hard-code `bg-red-500` etc.
- The palette resolves `user_preferences.fusePalette` overrides (see section 7). For now, pass `DEFAULT_FUSE_PALETTE`; section 7 wires the context.

### 6. MiroShark → Agent Desk rename (frontend + backend scope only)

TP: _"Remove the words MiroShark from Fintheon completely and replace it with Agent Desk."_

You own the rename in `frontend/**` and `backend-hono/**`. Session 2 owns the `mobile/**` rename.

What to change in frontend + backend:

- Rename folder `frontend/components/miroshark/` → `frontend/components/agent-desk/`. Rename the four component files inside by swapping `MiroShark` → `AgentDesk` in the filenames.
- Rename `frontend/types/miroshark.ts` → `frontend/types/agent-desk.ts`. Inside, rename all `MiroShark*` type names to `AgentDesk*`.
- Rename `backend-hono/src/services/miroshark/` → `backend-hono/src/services/agent-desk/`. Rename every file inside, type names, log strings.
- Rename `backend-hono/src/routes/miroshark/` → `backend-hono/src/routes/agent-desk/`. **Keep the HTTP route path `/api/miroshark/*` as a legacy alias in addition to a new `/api/agent-desk/*`** for the rest of v5.22 (so live mobile clients don't 404 during deploy). Register both paths; the handler can be shared.
- Audit every remaining "MiroShark" / "miroshark" / "miroShark" string in frontend + backend — all UI text becomes "Agent Desk", all variable names become `agentDesk`, all log lines become "Agent Desk".
- **Don't rename Supabase migrations** — filenames are historical. Don't rename DB columns/tables (`miroshark_deliberations`, etc.) in this sprint — defer that to a dedicated data migration sprint.
- **Don't touch `src/lib/changelog.ts` historical entries, `docs/sprint-briefs/ARCHIVE.md`, `docs/sprint-briefs/S20-*`, `docs/sprint-briefs/S23-*`, or any `.md` reference file** — those are historical records.
- `CLAUDE.md` in the repo root: leave alone (edit would affect both sessions).

Reference file list from pre-flight grep (80 files touched MiroShark; filter to frontend/ + backend-hono/src/ + minus historical docs):

- `frontend/contexts/SettingsContext.tsx`
- `frontend/components/chat/hooks/useHermesChat.ts`
- `frontend/components/narrative/SanctumBriefing.tsx`
- `frontend/components/narrative/Sanctum.tsx`
- `frontend/components/narrative/SanctumEconIntel.tsx`
- `frontend/components/narrative/SanctumTheses.tsx`
- `frontend/components/narrative/SanctumRiskAssessment.tsx`
- `frontend/components/narrative/BlendedVIXCard.tsx`
- `frontend/components/narrative/NextSessionForecastCard.tsx`
- `frontend/components/miroshark/*` (folder rename)
- `frontend/components/consilium/ConsiliumHub.tsx`
- `frontend/components/RiskFlowMini.tsx`
- `frontend/components/IVScoreCard.tsx`
- `frontend/components/apparatus/ApparatusMap.tsx`
- `frontend/components/apparatus/MemoryCard.tsx`
- `frontend/components/feed/AlertCardBase.tsx`
- `frontend/components/layout/FloatingWidget.tsx`
- `frontend/components/layout/TimelineOverlay.tsx`
- `frontend/components/settings/DeveloperTab.tsx`
- `frontend/components/onboarding/FirstTimeTour.tsx`
- `frontend/components/UpgradeModal.tsx`
- `frontend/types/miroshark.ts` (rename)
- `frontend/types/market-data.ts`
- `frontend/lib/apiClient.ts`
- `frontend/lib/internalConnectors.ts`
- `frontend/lib/services/index.ts`
- `frontend/lib/services/memory.ts`
- `backend-hono/src/routes/index.ts`
- `backend-hono/src/routes/harper/index.ts`
- `backend-hono/src/routes/miroshark/*` (folder rename + keep /api/miroshark alias)
- `backend-hono/src/routes/ai/handlers/chat.ts`
- `backend-hono/src/routes/data/index.ts`
- `backend-hono/src/routes/boardroom/index.ts`
- `backend-hono/src/routes/market-data/handlers.ts`
- `backend-hono/src/routes/market-data/index.ts`
- `backend-hono/src/routes/mcp/index.ts`
- `backend-hono/src/boot/services.ts`
- `backend-hono/src/services/strands/agents/harper.ts`
- `backend-hono/src/services/riskflow/central-scorer.ts`
- `backend-hono/src/services/feature-flag-service.ts`
- `backend-hono/src/services/agent-memory/outcome-tracker.ts`
- `backend-hono/src/services/miroshark/*` (folder rename)
- `backend-hono/src/services/harper-handler.ts`
- `backend-hono/src/services/cron/dispatch-scheduler.ts`
- `backend-hono/src/services/agent-bus/templates/miroshark-template.ts` (rename file too)
- `backend-hono/src/services/autoresearch/reflect-engine.ts`
- `backend-hono/src/services/market-data/iv-scorer.ts`
- `backend-hono/src/services/market-data/iv-score-ticker.ts`
- `backend-hono/src/services/supabase-service.ts`
- `backend-hono/src/services/agent-bus/types.ts`
- `backend-hono/.env.example`

### 7. Cross-platform settings sync — backend + desktop half

The contract files are already landed at commit `06a777a`:

- [frontend/lib/user-preferences.ts](frontend/lib/user-preferences.ts) — `UserPreferences`, `DEFAULT_PREFERENCES`, `PREFERENCES_API_PATH`
- [frontend/lib/fuse-palette.ts](frontend/lib/fuse-palette.ts) — palette types + defaults

You build:

- **Supabase migration** — audit for existing `profiles` table first. If it exists, add column `preferences jsonb not null default '{}'`. If not, create `user_preferences` table (`user_id uuid pk references auth.users(id)`, `prefs jsonb not null default '{}'`, `updated_at timestamptz not null default now()`). Migration is reversible.
- **New route** `backend-hono/src/routes/preferences.ts`: `GET /api/preferences` (returns the row for the authed user, falls back to `DEFAULT_PREFERENCES`), `PUT /api/preferences` (body validated via Zod matching `UserPreferences`; upserts the row). Auth via existing Supabase JWT middleware.
- Register the route in `backend-hono/src/routes/index.ts`.
- **Extend [frontend/contexts/SettingsContext.tsx](frontend/contexts/SettingsContext.tsx)** to:
  - On mount, fetch `GET /api/preferences`, merge into context.
  - On any setting change (theme, traderName, notifications, fusePalette), PUT back optimistically.
  - Poll `GET /api/preferences` every 30s for cross-device updates. If the remote `updatedAt` is newer than local, swap. No realtime/WS for v5.22 — polling only.
- Theme selector in settings writes to `preferences.theme`. Keep backward-compat with `localStorage` as a fallback for unauthed sessions.

Leave [mobile/contexts/SettingsContext.tsx](mobile/contexts/SettingsContext.tsx) **alone** — Session 2 wires the mobile side.

## Scope — Excluded (DO NOT TOUCH)

- Anything under `mobile/**` — that's Session 2.
- The shared contract files at `frontend/lib/fuse-palette.ts` and `frontend/lib/user-preferences.ts` — read-only. If you need to extend them, add fields at the END and mirror to `mobile/lib/*` in a separate commit so Session 2 can rebase cleanly.
- Supabase migration files named `*miroshark*` — historical, do not rename.
- `docs/sprint-briefs/S*.md` — historical sprint briefs.
- `src/lib/changelog.ts` entries dated before 2026-04-19T23:00. Your new entry goes at the top.
- CSS variables inside `@media (prefers-contrast: more)` at [frontend/index.css:1214-1227](frontend/index.css:1214) — that's the existing Nothing mode and it's load-bearing.
- `CLAUDE.md` (root and subproject). Historical records.
- Backend database columns/tables named `miroshark*` — renaming them needs its own migration sprint.
- Session 2's owned files: `mobile/components/chat/**`, `mobile/components/home/**`, `mobile/components/notifications/**`, `mobile/components/catalyst-modal/**`, `mobile/components/shared/IVFuseBar.tsx`, `mobile/components/shared/VerticalFuseBar.tsx`, `mobile/components/settings/**`, `mobile/contexts/SettingsContext.tsx`, `mobile/hooks/useMirosharkLatest.ts`.

## Known Issues to Preserve

- Existing shimmer animations (`fuse-shimmer`, `riskflow-fuse`, `severe-shimmer`, `blindspot-fuse-sweep`, `text-shimmer`, `agent-shimmer`) at [frontend/index.css:152-334](frontend/index.css:152) and [942-949, 1125-1131](frontend/index.css:942) — leave them. Your `nothing-fuse-shimmer` is additive.
- Nothing-mode flat CSS at [frontend/index.css:1214-1227](frontend/index.css:1214) is triggered by `@media (prefers-contrast: more)`. Don't repurpose it.
- Relay WS client ping keepalive landed in v5.21.2 — don't regress.
- S26 v.26.2 shipped catalyst modal + IV drain/fill choreography on mobile. Don't rework.

## Implementation Steps

1. Read the whole brief, `src/lib/changelog.ts` top 3 entries, `CLAUDE.md` root and subproject.
2. `cd /Users/tifos/Desktop/Codebases/fintheon-v5.22-s1`.
3. Start with section 6 (MiroShark → Agent Desk rename) — mechanical, lets you walk the codebase. Commit when tsc is clean.
4. Section 2 (shimmer keyframe + Nothing flatten) next — lands the CSS and shared class first so other sections can apply it.
5. Sections 1, 3, 4 (Sanctum refit + Deliberation footer + Econ/Instrument split) — touches `Sanctum.tsx` heavily; batch the edits.
6. Section 5 (RiskFlow desktop card refactor).
7. Section 7 (preferences backend + SettingsContext) — Supabase migration + route + context. Restart local backend (`launchctl unload io.solvys.fintheon-backend && launchctl load io.solvys.fintheon-backend`) and verify `/api/preferences` 200 before marking done.
8. `find frontend/dist -mindepth 1 -delete 2>/dev/null; cd frontend && npx vite build` — must be clean. Same for backend: `cd backend-hono && bun run build`.
9. Add one changelog entry for the entire session's work at `src/lib/changelog.ts`. Use date `2026-04-1{9,20}T{HH}:{mm}:00`. Include every file touched.
10. Merge back into `v5.22` on the main worktree (`/Users/tifos/Desktop/Codebases/fintheon`):
    ```
    cd /Users/tifos/Desktop/Codebases/fintheon
    git merge --no-ff v5.22-s1-desktop -m "v5.22: merge S1 desktop+backend"
    ```
11. Post a debrief to TP summarizing what landed, what didn't, and anything Session 2 now needs to rebase on.

## Acceptance Criteria

- [ ] `grep -r "MiroShark\|miroshark\|miroShark" frontend backend-hono/src` returns zero hits in live code (historical comments/changelog/archived docs exempted).
- [ ] Aquarium KPI row spans the same inline edges as the top frame at dash-page margins, no visible vertical dividers.
- [ ] ANALYSIS section sits in its own framed container, same width.
- [ ] /NQ /ES /YM /CL /GC headers align on a single row.
- [ ] Deliberation card footer shows SIGNAL STRENGTH / REGIME RISK / MARKET HEAT fuses.
- [ ] Sanctum section has Econ Intel left, compact Instrument fuses right.
- [ ] Desktop RiskFlow cards match mobile's visual vocabulary but larger.
- [ ] Every desktop fuse bar uses `.nothing-fuse-shimmer` and pulls color from `colorForSeverity()` / `colorForPriority()`. No tailwind color classes on fuse fills.
- [ ] Shimmer cycles exactly every 4.2s with ~1s dwell. `prefers-reduced-motion: reduce` hides it.
- [ ] `GET /api/preferences` returns 200 with a valid JWT; `PUT /api/preferences` round-trips. Migration is reversible.
- [ ] Frontend `SettingsContext` reads + writes to the new endpoint, polls every 30s.
- [ ] `npx tsc --noEmit --project frontend/tsconfig.json` clean.
- [ ] `cd backend-hono && bun run build` clean.
- [ ] `cd frontend && find dist -mindepth 1 -delete; npx vite build` clean.
- [ ] Local backend restarted via `launchctl unload/load io.solvys.fintheon-backend`; `/api/diagnostics` + `/api/preferences` both 200.
- [ ] One new changelog entry added.

## Validation Commands

```bash
# Rename-complete check (should return 0 real hits after excluding historical)
grep -rln "MiroShark\|miroshark\|miroShark" frontend backend-hono/src | grep -v "changelog\|ARCHIVE\|S20-\|S23-\|.sql$"

# Type check
npx tsc --noEmit --project frontend/tsconfig.json

# Frontend build
cd frontend && find dist -mindepth 1 -delete 2>/dev/null; npx vite build

# Backend build
cd backend-hono && bun run build

# Backend restart + verify
launchctl unload ~/Library/LaunchAgents/io.solvys.fintheon-backend.plist 2>/dev/null
launchctl load ~/Library/LaunchAgents/io.solvys.fintheon-backend.plist
curl -s http://localhost:8080/api/diagnostics | jq .
```

## Commit Format

One consolidated commit at end-of-session (after all sections land and validate):

```
v5.22 S1: desktop refit + backend preferences + MiroShark rename

- Sanctum KPI row + ANALYSIS frame at dash margins, no vertical dividers
- Deliberation card footer fuses (SIGNAL / REGIME / HEAT)
- Econ Intel left + Instrument fuses right
- Desktop RiskFlow card refactor (mobile-style, larger, severity-themed)
- Nothing-fuse-shimmer keyframe + applied to all desktop fuses
- MiroShark → Agent Desk rename across frontend/ + backend-hono/src/
- GET/PUT /api/preferences + Supabase user_preferences migration
- SettingsContext extended to read/write + poll every 30s

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

Use intermediate checkpoints during the session (auto-checkpoint commits are fine) but squash/reset to a single clean commit before merging to `v5.22`.

## Handoff to Session 2 / Unification

After your work merges to `v5.22`, Session 2 will rebase its `v5.22-s2-mobile` branch on top and complete the mobile side. You do not need to wait — just merge and post the debrief. TP will coordinate the unification pass.
